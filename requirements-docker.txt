# Production-only dependencies for rag-law API server
# Models (LLM, embedding, reranker) run on macOS via Ollama / reranker service
# Vector DB is a separate Qdrant server pod

# Web framework
fastapi==0.135.3
uvicorn[standard]==0.44.0
jinja2==3.1.6
python-multipart==0.0.24

# HTTP
requests==2.33.1
httpx==0.27.2

# Vector DB client (server mode, no embedded)
qdrant-client==1.17.1

# Korean BM25 search
kiwipiepy==0.23.1
kiwipiepy_model==0.23.0
rank-bm25==0.2.2

# CRAG pipeline (LangGraph state machine)
langgraph==1.1.6

# Utilities
python-dotenv==1.0.1

# Gemini API (최종 답변 생성)
google-generativeai>=0.8.0
